{
  "name": "@markpoint/powerpoint",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "dev": "tsc && tsc -w",
    "build": "tsc -p tsconfig.build.json",
    "lint": "prettier -c . && eslint . --cache --max-warnings=0 && tsc --noEmit"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.8.10",
    "jszip": "^3.10.1",
    "pptx-automizer": "^0.4.7"
  }
}
